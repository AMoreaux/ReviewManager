{% extends 'EmiageReviewManagerBundle::layout.html.twig' %}

{% block content -%}

    <h1>Examen list</h1>

    {% if entities.haveToPaginate %}
        {{ pagerfanta(entities, 'default', {'examen': 'page'}) }}
    {% endif %}

    {% if form is defined %}
        <form class="input-group input-group-lg" action="{{ path('examen') }}" method="post">
            <span class="input-group-addon">
                {{ form_widget(form) }}
                <input type="submit" value="Rechercher" />
            </span>
        </form>
    {% endif %}

    <table class="table">
        <thead>
            <tr>
                {% if is_granted ('ROLE_SUPER_ADMIN') %}
                    <th>Id</th>
                {% endif %}
                <th>Code</th>
                <th>Module</th>
                <th>ReviewCenter</th>
                <th>Realizedon</th>
                {% if (is_granted("ROLE_ADMIN")) or (is_granted("ROLE_PROF")) %}
                    <th>Student</th>
                {% endif %}
                {% if (is_granted("ROLE_ADMIN")) or (is_granted("ROLE_PROF")) %}
                    <th>Actions</th>
                {% endif %}
                {% if is_granted("ROLE_STUD") %}
                    <th>S'inscrire</th>
                {% endif %}
                {% if is_granted("ROLE_ADMIN") %}
                    <th>Imprimer</th>
                {% endif %}
            </tr>
        </thead>
        <tbody>
        {% for entity in entities.currentPageResults %}
            <tr>
                {% if is_granted ('ROLE_SUPER_ADMIN') %}
                    <td><a href="{{ path('examen_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                {% endif %}
                <td>{{ entity.code }}</td>
                <td>{{ entity.module.code }} {{ entity.module.name }}</td>
                {% if entity.reviewCenter is not null %}
                    <td>{{ entity.reviewCenter.name }}</td>
                {% else %}
                    <td>empty</td>
                {% endif %}
                {% if entity.Realizedon is not null %}
                    <td>{{ entity.realizedOn|date('Y-m-d H:i:s') }}</td>
                {% else %}
                    <td>empty</td>
                {% endif %}
                {% if (is_granted("ROLE_ADMIN")) or (is_granted("ROLE_PROF")) %}
                    <td>
                        <ul>
                            {% for student in entity.students %}
                                <li>{{ student.name }}</li>
                            {% endfor %}
                        </ul>
                    </td>
                {% endif %}
                {% if (is_granted("ROLE_ADMIN")) or (is_granted("ROLE_PROF")) %}
                    <td>
                        <ul>
                            <li>
                                <a href="{{ path('examen_edit', { 'id': entity.id }) }}" class="btn btn-default">edit</a>
                                <a href="{{ path('examen_delete', { 'id': entity.id }) }}" class="btn btn-default" onclick="return confirm('Supprimer cet examen ?');">delete</a>
                            </li>
                        </ul>
                    </td>
                {% endif %}
                {% if is_granted("ROLE_STUD") %}
                    <td>
                        <form action="{{ path('examen_addStudent',{ 'id': entity.id }) }}" >
                            <input type="submit" value="s'inscrire" class="btn btn-default"/>
                        </form>
                    </td>
                {% endif %}
                {% if is_granted("ROLE_ADMIN") %}
                    <td><a href="{{ path('examen_print', { 'id': entity.id }) }}" class="btn btn-default">Imprimer le PV</a></td>
                {% endif %}
            </tr>
        {% endfor %}
        </tbody>
    </table>
    {% if is_granted("ROLE_ADMIN") %}
        <ul>
            <li>
                <a href="{{ path('examen_new') }}" class="btn btn-primary">
                    Create a new entry
                </a>
            </li>
        </ul>
    {% endif %}

    {% if entities.haveToPaginate %}
        {{ pagerfanta(entities, 'default', {'examen': 'page'}) }}
    {% endif %}

    {% endblock %}
