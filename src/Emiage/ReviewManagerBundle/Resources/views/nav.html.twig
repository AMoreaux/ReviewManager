<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="topnavbar">
            <div class="nav-bar">
                <ul class="nav navbar-nav">
                    <li>
                        <a href="{{ path('examen') }}">Examen</a>
                    </li>
                    {% if is_granted('ROLE_ADMIN') and is_granted('ROLE_PROF') %}
                        <li>
                            <a href="{{ path('level') }}">Niveau</a>
                        </li>
                    {% endif %}
                    <li>
                        <a href="{{ path('homeModule') }}">Module</a>
                    </li>
                    <li>
                        <a href="{{ path('note') }}">Note</a>
                    </li>
                    {% if is_granted('ROLE_ADMIN') and is_granted('ROLE_PROF') %}
                        <li>
                            <a href="{{ path('registercenter') }}">Centre nationaux</a>
                        </li>
                    {% endif %}
                    {% if is_granted('ROLE_ADMIN') and is_granted('ROLE_PROF') %}
                        <li>
                            <a href="{{ path('reviewcenter') }}">Centre d'examen</a>
                        </li>
                    {% endif %}
                    {% if is_granted('ROLE_ADMIN')%}
                        <li>
                            <a href="{{ path('student') }}">Etudiant</a>
                        </li>
                    {% endif %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <li>
                            <a href="{{ path('tutor') }}">Tuteur</a>
                        </li>
                    {% endif %}
                    {% if is_granted('ROLE_ADMIN') and is_granted('ROLE_PROF') %}
                        <li>
                            <a href="{{ path('user') }}">Utilisateur</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>