{% extends 'EmiageReviewManagerBundle::layout.html.twig' %}

{% block content -%}
    <h1>Liste niveaux</h1>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.name }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('level_edit', { 'id': entity.id }) }}" class="btn btn-default">edit</a>
                        <a href="{{ path('level_delete', { 'id': entity.id }) }}" class="btn btn-default" onclick="return confirm('Supprimer ce niveau ?');">supprimer</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('level_new') }}" class="btn btn-primary">
                Créer un niveau
            </a>
        </li>
    </ul>
    {% endblock %}
